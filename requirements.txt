# Runtime dependencies for the Polymarket bot.
# Unpinned — no lockfile is maintained; install with `pip install -r requirements.txt`.

# HTTP + market data
requests

# Polymarket CLOB trading client
py-clob-client

# Gasless redemption via the Builder Relayer
py-builder-signing-sdk
web3
eth-account
eth-abi

# News + research
feedparser
ddgs
python-dateutil
